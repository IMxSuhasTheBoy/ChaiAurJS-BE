////--------------syntax playground for imports/exports

// export const add = (a, b) => a + b;
// export const subtract = (a, b) => a - b;

const math = {
  add: (a, b) => a + b,
  mul: (a, b) => a * b,
  mod: (a, b) => a % b,
};
export default math;
